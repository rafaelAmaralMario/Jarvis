export interface AppMetadata {
  name: string;
  description: string;
}

export const appMetadata: AppMetadata = {
  name: 'JARVIS',
  description: 'IDE desktop pensada para modelos, agentes e contexto local.',
};

