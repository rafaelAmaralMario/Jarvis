#include "jarvis/core/module_loader.h"
#include "jarvis/core/service_locator.h"

// Lifecycle management for the JARVIS kernel.
// This file serves as a placeholder for application-wide lifecycle coordination
// beyond what ModuleLoader provides (pre-init, post-shutdown hooks, etc.).
// Currently, lifecycle is handled directly in main.cpp via ModuleLoader.
