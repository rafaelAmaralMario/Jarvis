{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main JARVIS window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
